type: fixed
title: The README, the docs landing page and the MkDocs favicon and logo reference the PNG assets under their real .png names; the five docs/assets/*.svg files that held PNG bytes are removed, and make docs-assets-test (part of verify-all) fails when a tracked image's bytes do not match its extension or a referenced image is missing
breaking: false
