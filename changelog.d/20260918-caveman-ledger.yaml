type: changed
title: Write compact STATE.md entries without superseded markers, keep bug metadata in a bugs.meta.json sidecar, and add one-time `state compact` and `state migrate-bugs` commands; binaries older than this cannot read sidecar bug rows
breaking: true
