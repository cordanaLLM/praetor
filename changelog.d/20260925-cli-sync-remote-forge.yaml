type: fixed
title: sync --remote refuses an origin on another host or a local path (new --forge-host flag, default github.com), so acme/widgets on gitlab.com no longer authorizes a GitHub ruleset write; it also writes the .config/labels.yaml taxonomy to GitHub instead of only validating the local file
breaking: false
