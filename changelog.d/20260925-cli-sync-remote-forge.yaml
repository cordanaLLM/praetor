type: fixed
title: sync --remote now writes the same ruleset as .github/rulesets/main.json (main and lts-*), merges it into the live GitHub ruleset without dropping extra refs, rules, parameters, bypass actors or required checks, and reads it back; it also writes the .config/labels.yaml taxonomy to GitHub, and refuses an origin on another host or a local path (new --forge-host flag, default github.com)
breaking: false
