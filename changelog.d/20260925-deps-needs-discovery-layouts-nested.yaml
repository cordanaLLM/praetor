type: fixed
title: needs discovery makes every git checkout a repository row of its own (submodules and independent clones nested in another checkout included), collapses linked worktrees of one repository, and the submodules they check out, into one row and lists them, never lets a manifest or declaration directory hide a checkout below it, folds nested sub-projects into their repository's row for scan, report, MCP standards_needs_report, aggregate and epics alike so epic and aggregate readiness agree, reports sub-projects deeper than five directories instead of dropping them, lists a nested sub-project whose scan fails on its row instead of failing the whole repository, stops merging same-named repositories, walks first-party Build-tools/ and build_scripts/ trees, fails fleet epics for repositories that declare needs but hold no analyzable project, and counts PyPI names that differ only by PEP 503 normalisation once
breaking: false
