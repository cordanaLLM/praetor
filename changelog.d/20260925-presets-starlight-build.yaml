type: fixed
title: Make the Starlight preset build, with a shipped hero image, one page in each autogenerated sidebar group (including the onboarding page the hero links to) and the Content Layer docsLoader() collection config, render its SEOHead component as a Starlight Head override that adds a per-page TechArticle after the default head, and drop the stylesheet rules and font preconnects that had no effect
breaking: false
