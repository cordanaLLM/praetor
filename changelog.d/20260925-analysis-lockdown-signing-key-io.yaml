type: fixed
title: Refuse a receipt signing key file larger than 4096 bytes instead of loading its truncated prefix, and create the key with gate keygen through an exclusive, fsynced write so two concurrent runs cannot both succeed, an existing key or a symbolic link at the key path is never overwritten or followed, and a crash cannot leave a truncated key behind
breaking: false
