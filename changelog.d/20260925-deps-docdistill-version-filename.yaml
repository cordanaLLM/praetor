type: fixed
title: Sanitize the dependency version as well as the package name in distilled-doc filenames, so a package.json version such as file:../../x can no longer place a file outside the distilled directory or abort docs sync
breaking: false
