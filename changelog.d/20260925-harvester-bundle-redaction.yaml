type: fixed
title: Redact credential values (token, secret, password, apiKey, Authorization, env and bearer strings) from agent and MCP configuration JSON before it enters a harvest bundle, skip configuration JSON that does not parse instead of copying it verbatim, count claude_desktop_config.json as credential-bearing, and print the redaction count
breaking: false
