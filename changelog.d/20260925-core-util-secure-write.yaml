type: fixed
title: util.ConfinePath accepts paths under a root of "/" or a Windows volume root, returns the checked location with in-root symlinked directories resolved, and refuses a parent directory outside the root; WriteFileNoFollow writes atomically through a pinned directory handle, so a failed or interrupted write leaves the previous milestones.json, BACKLOG.md or project.json intact, and a hard link at the destination or a symbolic link planted after the check is replaced instead of written through
breaking: false
