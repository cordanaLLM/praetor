type: fixed
title: Confine ledger, project-cache, onboarding, wiki and ADR writes to the repository through a pinned directory handle, accept paths under a "/" or volume root, and replace ledgers atomically; a relative forge sync-wiki output must now stay inside the repository, and a write-protected ledger is refused instead of replaced
breaking: false
