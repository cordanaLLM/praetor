type: fixed
title: Run the dedupe scan's file inventory and the dogfood ephemeral clone under an isolated Git configuration, restrict remote dogfood targets to https URLs (ssh:// is no longer accepted, because the sandbox carries no credentials), fail the dedupe verdict on any duplicate or utility-sprawl finding, and report exec.CommandContext git calls as sprawl
issue: "#295"
breaking: true
