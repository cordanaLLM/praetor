type: fixed
title: flavor apply scaffolds real files from the embedded templates/ tree (CI and security workflows, a multi-stage distroless Dockerfile, a gitleaks config that extends the default rules, ruff, yamllint, Rust, Node, JVM and Flutter configs) instead of one-line comment placeholders, leaves adopt- and compile-context-owned files to their producer, and flavor audit now counts a template only as a regular file whose content passes its validator, so a repository still carrying an earlier placeholder reports it missing until it is replaced
breaking: true
