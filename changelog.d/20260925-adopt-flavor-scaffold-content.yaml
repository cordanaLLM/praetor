type: fixed
title: flavor apply scaffolds real files from the embedded templates/ tree (CI and security workflows, a multi-stage distroless Dockerfile, a gitleaks config that extends the default rules, ruff, yamllint, Rust, Node, JVM and Flutter configs) instead of one-line comment placeholders, leaves adopt- and compile-context-owned files to their producer, and flavor audit now counts a template only as a regular file whose content passes its validator, so a repository still carrying an earlier placeholder reports it missing until it is replaced; the scaffolded Go CI job runs go vet and go test -race instead of a make verify-all no step could run, the Go Dockerfile builds the module's only main package (or the one MAIN_PACKAGE names) instead of ./cmd/..., flavor apply writes no rival beside an existing alternative config, adopt scaffolds only a flavor detection names instead of falling back to go-library, typescript-node scaffolds its npm CI job only where package-lock.json and a real test script exist and packageManager names npm or nothing (elsewhere flavor apply lists it under Unmet Requirement and adopt warns, so no unpassable Node check is required), and adoption's verification plan no longer accepts the placeholder test script npm init writes
breaking: true
