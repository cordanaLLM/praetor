type: added
title: Add --max-archive-size to praetorctl devsync push so an oversized project folder is skipped and reported instead of uploaded, with a summary of what the cap skipped
breaking: false
