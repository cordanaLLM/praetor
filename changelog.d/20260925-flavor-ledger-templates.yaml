type: fixed
title: "Stop requiring the git-ignored .workingdir ledgers as flavor templates, so fresh clones and linked worktrees of conforming repositories pass the flavor audit and gate stage, and count only files, not directories, as satisfied templates. This tightens the bar on checkouts that carried the ledger: its three files no longer count as present templates, so python-ml, os-image, frontend-svelte, jvm-service, mobile-flutter and agentic-autonomous now fail on one missing or invalid setting, and go-service on two"
issue: "#72"
breaking: true
