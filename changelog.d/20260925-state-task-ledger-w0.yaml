type: fixed
title: Make state task numbering and selectors exact and fence-aware, refuse an OPEN.md with an unterminated code fence instead of silently dropping every row after it, seed a ledger without fabricated rows into a directory another command created, report what state init --if-absent actually did, publish each seeded ledger file atomically, and make standalone state init, and every command that seeds a missing ledger on first use (state sync, state task add, state bug add and resolve, state question add, flavor apply), add the private-artifact block to .gitignore when Git does not already ignore .workingdir
issue: "#286"
breaking: true
