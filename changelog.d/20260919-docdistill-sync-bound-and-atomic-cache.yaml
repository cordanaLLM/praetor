type: fixed
title: "`docs sync` now bounds its harvest loop and deadline on its own (`DistillOptions.MaxPackages`/`Timeout`, reporting truncation) and writes the catalog and every distilled doc atomically, so an interrupted write can no longer leave a zero-length cache"
breaking: false
