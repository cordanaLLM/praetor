type: fixed
title: issue reconcile refuses a --repos selection that names no repository instead of reporting success over nothing; baseline inspect no longer calls a stored or missing snapshot "100% compliant"; topology audit fails on DEV-01/DEV-02 invariant violations and its pass line names only the rules it evaluates
breaking: false
