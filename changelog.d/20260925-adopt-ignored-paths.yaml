type: fixed
title: Adoption reports every pinned catalog file the repository's .gitignore excludes (a kernel-style bare .config rule) instead of silently writing files git will not commit, and adopt verification planning, dedupe and the HISS scanner share one scratch-directory list so .claude worktree copies and .standards no longer exhaust the 4096-entry verification bound
breaking: false
