[project]
name = "ml-service"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "torch>=2.14.0",
    "numpy>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
